spring:
  datasource:
    url: jdbc:postgresql://dpg-d4f3m97pm1nc73em09e0-a.oregon-postgres.render.com:5432/ecommerce_db_0ywh
    username: ecommerce_db_0ywh_user
    password: GoRwBFaR2Uq8Y9jc6dzpsNRMl9ak7vEE
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update   # use 'validate' or 'none' in production
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

# Optional: Configure server port (default is 8080)
server:
  port: 8080

# Optional: Logging configuration
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
